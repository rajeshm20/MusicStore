# Uncomment the next line to define a global platform for your project
 platform :ios, '9.0'

target 'MusicStore' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MusicStore
	pod 'GRDB.swift/SQLCipher'
	pod 'SQLCipher', '~> 4.0'
  target 'MusicStoreTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MusicStoreUITests' do
    # Pods for testing
  end

end
